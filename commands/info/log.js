const axios = require('axios');
const { MessageEmbed } = require('discord.js');

module.exports = {
  name: 'log',
  category: 'fun',
  aliases: ['avt'],
  run: async (client, message, args) => {},
};
